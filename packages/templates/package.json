{
  "name": "@regex-studio/templates",
  "version": "3.0.0",
  "description": "Pre-compiled regular expression template registry library for Regex Studio",
  "license": "Apache-2.0",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "types": "./src/index.ts",
  "dependencies": {
    "@regex-studio/regex-core": "*"
  },
  "scripts": {
    "build": "tsc --noEmit",
    "typecheck": "tsc --noEmit",
    "lint": "tsc --noEmit"
  },
  "publishConfig": {
    "registry": "https://npm.pkg.github.com"
  }
}
